$(document).ready(function () {
    debugger;
    //Guyu.LightBox.showMessageBox('Cancel configuration', 'Do you want to cancel all configurations?', {
    //    showOk: true,
    //    showCancel: true,
    //    messageBoxType: 'warning'
    //});

    //var a = Guyu.fetch('http://localhost/api/agencies/1', { body: {}});
    //a.then(function (response) {
    //    var a = response;
    //})
});